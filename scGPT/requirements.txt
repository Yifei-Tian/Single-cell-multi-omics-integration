# ============================================================
# scGPT 多组学整合分析 — 依赖清单
# Python >= 3.10
# CUDA   >= 11.8
# 硬件最低要求：NVIDIA RTX 3090 (24 GB) 或 V100 (16 GB)
# ============================================================

# ---------- scGPT 核心 ----------
scgpt>=0.2.1

# ---------- 深度学习框架 ----------
torch>=2.0.0
torchvision>=0.15.0

# ---------- Hugging Face 生态（scGPT 内部依赖） ----------
transformers>=4.30.0
datasets>=2.12.0
huggingface_hub>=0.16.0

# ---------- 单细胞分析核心库 ----------
scanpy>=1.9.6
anndata>=0.9.2
muon>=0.1.5

# ---------- 多组学基准评估 ----------
scib>=1.1.4
scib-metrics>=0.3.3

# ---------- 机器学习与数值计算 ----------
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ---------- 可视化 ----------
matplotlib>=3.7.0
seaborn>=0.12.0

# ---------- 性能加速（可选，强烈推荐） ----------
# flash-attn>=2.0.0   # 需要 CUDA 11.8+，按需手动安装：pip install flash-attn --no-build-isolation

# ---------- 其他工具 ----------
tqdm>=4.65.0
leidenalg>=0.10.0
python-igraph>=0.10.0
